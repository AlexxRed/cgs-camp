import React from 'react';
import { ThreeCircles } from 'react-loader-spinner';

export const Loader = () => (
  <ThreeCircles color="#1a2869" height={80} width={80} wrapperStyle={{ margin: '0 auto' }} />
);
